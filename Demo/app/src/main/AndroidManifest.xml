<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.syz.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".util.MyAppcation"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homeFragment.gif.GifShowActivity" />
        <activity
            android:name=".screenPage.mineScreen.SettingActivity"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity
            android:name=".screenPage.mineScreen.Loginactivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme" />
        <activity android:name=".screenPage.mineScreen.RegisterActivity" />
        <activity
            android:name=".screenPage.mineScreen.PersonInfoActivity"
            android:label="">
        </activity>
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.cameraalbumtest.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity android:name=".screenpage.VideoShowActivity"></activity>
        <activity android:name=".screenPage.communityScreen.CommunityInfoActivity" />
        <activity android:name=".screenPage.mineScreen.ImgShowActivity" />
        <activity android:name=".screenPage.communityScreen.SearchActivity" />

        <activity android:name=".screenPage.FloatActivity"></activity>
        <activity
            android:name=".screenPage.PublishActivity.PublishActivity"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".screenPage.PublishActivity.SelectPlateActivity"></activity>
    </application>

</manifest>